// Dependencies
const express = require('express');

const app = express();
const PORT = 4000;

// Data


// Routes


// Listener
app.listen(PORT, () => {
    console.log(`App listening on PORT ${PORT}`);
})